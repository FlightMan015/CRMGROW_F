import { DateSpliterPipe } from './date-spliter.pipe';

describe('DateSpliterPipe', () => {
  it('create an instance', () => {
    const pipe = new DateSpliterPipe();
    expect(pipe).toBeTruthy();
  });
});
